{
  "mcpServers": {
    "pocketsmith-mcp": {
      "command": "npx",
      "args": ["pocketsmith-mcp"],
      "env": {
        "MCP_LOG_LEVEL": "debug",
        "MCP_TRANSPORT_TYPE": "stdio",
        "POCKETSMITH_API_KEY": "your_api_key_here"
      }
    }
  }
}
